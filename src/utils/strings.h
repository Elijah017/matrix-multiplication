#ifndef _MATRIX_STRINGS
#define _MATRIX_STRINGS

#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

#include "../config.h"

char *u64_to_string(void *value);

char *u32_to_string(void *value);

char *u8_to_string(void *value);

#endif
